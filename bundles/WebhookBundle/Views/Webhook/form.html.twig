{% extends 'MilexCoreBundle:Default:content.html.twig' %}

{% block headerTitle %}{{ entity.getId() ? 
    'milex.webhook.webhook.header.edit'|trans({'%name%': entity.getName()|trans}) :
    'milex.webhook.webhook.header.new'|trans }}{% endblock %}
{% block milexContent %}milexWebhook{% endblock %}

{% block content %}
{{ form_start(form) }}
<!-- start: box layout -->
<div class="box-layout">
    <!-- container -->
    <div class="col-md-9 bg-auto height-auto">
        <div class="pa-md">
            <div class="row">
                <div class="col-md-6">
                    {{ form_row(form.name) }}
                    {{ form_row(form.description) }}
                    {{ form_row(form.secret) }}
                    {{ form_row(form.webhookUrl) }}
                    <div class="row">
                        <div class="col-md-5">
                            {{ form_row(form.sendTest) }}
                        </div>
                        <div class="col-md-2">
                            <span id="spinner" class="fa fa-spinner fa-spin hide"></span>
                        </div>
                        <div class="col-md-5">
                            <div id="tester" class="text-right"></div>
                        </div>
                    </div>

                </div>
                <div class="col-md-6" id="event-types">
                    {{ form_row(form.events) }}
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-3 bg-white height-auto bdr-l">
        <div class="pr-lg pl-lg pt-md pb-md">
            {{ form_row(form.category) }}
            {{ form_row(form.eventsOrderbyDir) }}
            {{ form_row(form.isPublished) }}
        </div>
    </div>
</div>
{{ form_end(form) }}

{% endblock %}
